apiVersion: v1
kind: ConfigMap
metadata:
  name: observability-config
  namespace: observability
data:
  # Terraform outputs - replace these values
  AMP_WORKSPACE_ID: "<REPLACE_WITH_TERRAFORM_OUTPUT>"
  AMP_REGION: "ap-northeast-2"
  LOKI_ENDPOINT: "http://loki:3100"
