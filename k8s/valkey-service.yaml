apiVersion: v1
kind: Service
metadata:
  name: valkey
  namespace: queue-system
spec:
  type: ClusterIP
  selector:
    app: valkey
  ports:
    - port: 6379
      targetPort: 6379
      protocol: TCP
